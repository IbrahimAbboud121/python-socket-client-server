
## 🔧 Installation & Requirements

No external dependencies required! This project uses only Python's standard library.

```bash
# Clone the repository
git clone https://github.com/yourusername/python-socket-client-server.git
cd python-socket-client-server

# Python 3.6+ is required
python --version  # Should show Python 3.6 or higher
